# Aedelore Environment Configuration Template
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# ============================================
# Database Configuration
# ============================================
POSTGRES_DB=aedelore
POSTGRES_USER=aedelore
# Generate with: openssl rand -base64 32 | tr -d '/+=' | head -c 40
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_SECURE_PASSWORD

# Database URL for API (use same password as above)
DATABASE_URL=postgres://aedelore:CHANGE_ME_GENERATE_SECURE_PASSWORD@aedelore-proffs-db:5432/aedelore

# ============================================
# CORS Configuration
# ============================================
CORS_ORIGIN=https://aedelore.nu

# ============================================
# SMTP Configuration (Password Reset)
# ============================================
SMTP_HOST=smtp.protonmail.ch
SMTP_PORT=587
# Your email address (sender must match for Proton Mail)
SMTP_USER=your-email@example.com
# App-specific password (generate in your email provider's settings)
SMTP_PASS=CHANGE_ME_APP_PASSWORD
# From address (must match SMTP_USER for Proton Mail)
SMTP_FROM=YourApp <your-email@example.com>
# Your application URL (used in reset links)
APP_URL=https://your-domain.com

# ============================================
# Authentication Mode
# ============================================
# local  = Username/password only (default)
# oidc   = OIDC only (Keycloak, etc.)
# both   = Local + OIDC login options
AUTH_MODE=local

# ============================================
# OIDC Configuration (Optional)
# ============================================
# Supports multiple providers: OIDC_1_*, OIDC_2_*, OIDC_3_*, etc.

# OIDC Provider 1
#OIDC_1_ISSUER_URL=https://keycloak.example.com/realms/gaming
#OIDC_1_CLIENT_ID=aedelore
#OIDC_1_CLIENT_SECRET=              # Optional (leave empty for public client with PKCE)
#OIDC_1_PROVIDER_NAME=Gaming Realm  # Shown on login button

# OIDC Provider 2 (optional)
#OIDC_2_ISSUER_URL=https://keycloak.example.com/realms/friends
#OIDC_2_CLIENT_ID=aedelore
#OIDC_2_PROVIDER_NAME=Friends Realm

# Callback URL for OIDC (same for all providers)
#OIDC_CALLBACK_URL=https://your-domain.com/character-sheet

# ============================================
# Umami Analytics (Optional)
# ============================================
# Generate with: openssl rand -hex 32
UMAMI_DB_PASSWORD=CHANGE_ME_GENERATE_SECURE_HEX
UMAMI_APP_SECRET=CHANGE_ME_GENERATE_SECURE_HEX
